## Unreleased
- Markdown example: rewritten as a small package under `tests/examples/markdown/`
  (`types.ts`, `inline.ts`, `blocks.ts`, `references.ts`, `index.ts`) and brought
  to near-CommonMark coverage, all built from Tarsec combinators:
  - Inline: escape sequences, `*`/`_` italic, `**`/`__` bold, combined
    `***bold-italic***`, `~~strikethrough~~`, autolinks (`<http(s)://…>` and
    `<email>`), hard line breaks (two trailing spaces or `\`), inline images,
    and deferred reference-style links / images.
  - Blocks: ATX headings, setext headings, fenced code blocks (with
    `objective-c` / `c++` / `ts2` style language tags), indented code blocks,
    multi-line and nested block quotes, unordered / ordered / nested lists,
    pipe tables with alignment, horizontal rules, and HTML block passthrough.
  - Reference resolution: `[id]: url` link definitions and `[^id]: …`
    footnote definitions are collected and resolved in a post-parse pass over
    the AST.
  - VitePress-style YAML frontmatter: an optional `---`-delimited block at the
    top of the document. Supports `key: value` entries with bare/quoted
    strings, integers, floats, booleans, `null`/`~`, and inline flow lists.
  - Fixes: code-block language tags now accept hyphens / digits / `+`,
    paragraphs terminate at a blank line, bold-vs-italic precedence is correct,
    and inline text's stop set covers all the new delimiters.

## v0.0.19
- adds `captureCaptures`
